const slash = '/';
export const ROUTES = {
    LOGIN: 'login',
    COURSES: 'courses',
    COURSE: 'course'
};

export const ROUTES_SLASHED = {
    LOGIN: slash + ROUTES.LOGIN,
    COURSES: slash + ROUTES.COURSES,
    COURSE: slash + ROUTES.COURSE
};
